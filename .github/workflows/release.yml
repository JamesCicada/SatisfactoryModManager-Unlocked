name: Release

on:
  push:
    tags:
      - v*

permissions:
  contents: write

jobs:
  release:
    uses: ./.github/workflows/build-base.yml
    with:
      goreleaser_args: ''
    secrets: inherit
